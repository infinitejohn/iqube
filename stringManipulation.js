const str1 = "Hello, ";
const str2 = "world!";
const concatenated = str1 + str2; // Using the + operator for concatenation
console.log(concatenated);

const sliced = str2.slice(0, 3); // Extracting a portion of the string
console.log(sliced);

const replaced = str1.replace("Hello", "Hi"); // Replacing part of the string
console.log(replaced);
